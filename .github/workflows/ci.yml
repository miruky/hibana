name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          cache: npm
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: tools/blender/requirements-qa.txt
      - run: npm ci
      - name: Install Blender QA Python dependencies
        run: python -m pip install --disable-pip-version-check -r tools/blender/requirements-qa.txt
      - name: Validate Blender release assets
        run: npm run blender:validate
      - run: npm run lint
      - run: npm test
      - run: npm run build
      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium
      - name: Browser smoke (UI2 + classic, 720p + 1080p)
        run: npm run test:e2e
        env:
          UI2_SHOT_DIR: /tmp/hibana-e2e
      - name: Upload browser screenshots on failure
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: hibana-e2e-screenshots
          path: /tmp/hibana-e2e
          if-no-files-found: ignore
          retention-days: 7
